set 사용이 메모리를 vector에 비해 적게 먹는다.
하지만 시간을 기준으로 보았을 경우 60ms라는 명확한 시간차가 났으며
벡터의 경우 중복 제거와 제거하지 않았을 경우를 비교했을때 시간이 각각 100ms으로 동일 하였다.
(tc = 1000)

결론: 메모리가 중요하다면 set
중복을 별로 신경쓸 수준이 아니라면 시간상 벡터의 압승
+굳이 중복을 제거하지 않아도 된다.

추가: 연결없이 노드만 존제하는경우 N의 개수에는 포함되지만 연결되지 않았으므로 입력이 주어지지 않는다
(=노드를 적어놓는 리스트에 들어가지 않는다.)따라서 이를 방지하기 위해 전체 노드수 - 방문한 노드수를 계산하여 더해줘야 한다.